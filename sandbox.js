let age=25;
let year=2019;

console.log(age, year);

age=30;
console.log(age);

const points =100;
consol.log(points);

var score=75;
consol.log(score);

const password= 'password1234';
if(password.length >= 12 && password.include('@')){
  console.log('that pass is mighty strong');
} else{
  console.log('password is not long enough'); 
}
}
